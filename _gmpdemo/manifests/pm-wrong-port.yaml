apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: my-application
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: my-application # Fixed!
  endpoints:
  - port: metric # Still typo here!
    interval: 30s
